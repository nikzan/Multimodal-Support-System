package com.nova.support.domain.enums;

/**
 * Приоритет тикета (определяется AI)
 */
public enum Priority {
    LOW,      // Низкий приоритет (общие вопросы)
    MEDIUM,   // Средний приоритет (проблемы, требующие внимания)
    HIGH,     // Высокий приоритет (срочные проблемы)
    CRITICAL  // Критический (клиент очень недоволен, серьезная проблема)
}
